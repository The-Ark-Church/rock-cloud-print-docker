# Build output
**/bin/
**/obj/

# User secrets / runtime config
# This file holds the live Rock server URL, proxy ID, and PIN.
# It is volume-mounted into the container and should never be committed.
config/appsettings.json

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Web UI build output
# app.css is generated from build/src/app.css by `npm run css` (and by the
# Docker build). Generated, so not committed.
node_modules/
Rock.CloudPrint.Service/wwwroot/app.css
